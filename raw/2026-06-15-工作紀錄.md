# 2026-06-15 工作紀錄

## My-Agent 更新與修復

### 1. 版本更新
- 將 `C:\Users\kongwang\Tools\my-agent` 從舊版更新為新版 (VERSION 1.0.0)
- 來源：`C:\Users\kongwang\Tools\my-agent (2)\my-agent`
- 保留了 `backend/memory.db`（對話紀錄）和 `backend/config.json`（設定含 Telegram token、Cursor API key）
- 重新安裝了 Python 依賴（.venv）和前端 npm 套件

### 2. 加入 shell tool
- 在 `backend/app/tools.py` 新增 `shell` tool（有 timeout 限制、輸出截斷）
- 但發現 kiro provider 走 CLI agent，不使用 tools.py

### 3. 啟用 kiro-cli 完整工具權限
- 修改 `backend/app/providers/cli_agent_provider.py`
- kiro preset 加入 `--trust-all-tools`，讓 TG Bot 能透過 kiro-cli 執行 shell 等操作（如開瀏覽器）

### 4. 修復 WinError 206（檔名太長）
- 問題：對話歷史累積後，prompt 作為命令列參數超過 Windows 32K 限制
- 修正：在 `cli_agent_provider.py` 加入 `_truncate_prompt()` 函數
- 當 prompt > 28000 字元時，截斷中間舊歷史，保留 system prompt 和最新訊息

### 5. Telegram Bot 安全設定
- 已設定 `allowed_users` 限制只有指定使用者可使用 bot
- 未授權使用者發訊息會被靜默忽略

## 狀態
- Agent 已正常運行中
- Provider: kiro (kiro-cli with --trust-all-tools)
- Web UI: http://localhost:13301
- Telegram: @IgsMoAssistantBot
